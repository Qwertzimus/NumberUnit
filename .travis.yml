env:
  global:
    - CC_TEST_REPORTER_ID=3fa38310293a9b102fbbae86652340c2770749314914f94d8b919c26fd246be0
language: ruby
rvm:
  - 2.2.0
before_script:
  - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
  - chmod +x ./cc-test-reporter
  - ./cc-test-reporter before-build
after_script:
  - ./cc-test-reporter after-build --exit-code $TRAVIS_TEST_RESULT

install:
- rm -r -f "$HOME/.m2/repository"
- git clone "https://github.com/EmbeddedMontiArc/external-dependencies" "$HOME/.m2/repository"

script:
- mvn clean install --settings "./settings.xml"
after_success:
- mvn deploy --debug --settings "./settings.xml"
